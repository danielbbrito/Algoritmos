#include <stdio.h>

int main(void)
{
    // Declarar variáveis
    int A, B;

    // Pegar input
    scanf("%d %d", &A, &B);

    // Somar
    int soma = A + B;

    // Output
    printf("SOMA = %i\n", soma);

    return 0;
}